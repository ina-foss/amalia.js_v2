import {AmaliaException} from '../../exception/amalia-exception';

/**
 * In charge to load metadata with ws socket
 */
export class WSLoader {
  constructor() {
    throw new AmaliaException('Not implemented');
  }
}
