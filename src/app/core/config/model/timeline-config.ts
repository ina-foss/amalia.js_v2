export interface TimelineConfig {
    /**
     * True for enable resize plugin
     */
    resizeable: boolean;
    /**
     * Time format
     */
    timeFormat: 'h' | 'm' | 's' | 'f' | 'ms' | 'mms' | 'seconds';
}
